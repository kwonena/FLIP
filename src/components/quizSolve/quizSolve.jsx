import React from "react";
import styles from "./quizSolve.module.css";

// header에서 로그인 안 보이게
const QuizSolve = (props) => {
  return <h1 className={styles.q}>quizSolve</h1>;
};

export default QuizSolve;
